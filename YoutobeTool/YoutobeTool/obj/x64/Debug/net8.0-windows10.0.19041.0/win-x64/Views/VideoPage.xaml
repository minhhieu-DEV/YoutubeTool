<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="YoutobeTool.Views.VideoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YoutobeTool.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Margin="20" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="32"/>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Xử lý video" Style="{StaticResource TitleTextBlockStyle}"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:ConnectionId='3' Padding="10" VerticalAlignment="Center" x:Name="txtPathImages" Text="{Binding PathFolder}" />
            <Button x:ConnectionId='4' HorizontalAlignment="Right"  Grid.Column="1" Command="{Binding ChooseFolderClicked}" x:Name="btnFolderImages" Style="{StaticResource AccentButtonStyle}" Content="Chọn folder chứa ảnh"/>
        </Grid>
        <ComboBox Grid.Row="2" ItemsSource="{Binding Features}" SelectedValue="{Binding Feature, Mode=TwoWay}" PlaceholderText="Chọn hiệu ứng cho video"/>
        <controls:DataGrid Grid.Row="3" AutoGenerateColumns="False" ItemsSource="{Binding FolderModels}">
            <controls:DataGrid.Resources>
                <Style TargetType="controls:DataGridCell">
                    <Setter Property="FontSize" Value="13"/>
                </Style>
            </controls:DataGrid.Resources>
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="STT" Binding="{Binding Index}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Tên folder" Binding="{Binding Name}" Width="Auto"/>
                <controls:DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="*"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
        <Rectangle Grid.Row="4" Height="3" Fill="LightGray"/>
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Status}"/>
            <Button x:ConnectionId='2' HorizontalAlignment="Right"  Grid.Column="1" Command="{Binding CreateVideoByImageClicked}" x:Name="btnCreateVideoTotal" Style="{StaticResource AccentButtonStyle}" Content="Tạo video tổng"/>
        </Grid>
    </Grid>
</Page>

